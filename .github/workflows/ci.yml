name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Print Hello World
      run: echo "Hello, World!"
    - name: Run simple Node.js script
      run: node index.js